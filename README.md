# Step:

### 1. Install Node.js v22.x latest version

### 2. Rename the .env.example to .env

### 3. run command to install package
```JavaScript
npm install
```

### 4. run command to build the source
```JavaScript
npm run build
```

### 5. run command to start the UI server
```JavaScript
npm start
```